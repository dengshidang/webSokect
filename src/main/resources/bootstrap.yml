spring :
 application :
    name : wechat-server
 jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

 datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username:
    password:
    url:
    druid:
      initial-size: 1
      min-idle : 1
      max-active:
      test-on-borrow:
      stat-view-servlet.allow:

